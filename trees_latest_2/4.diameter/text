so logic is that left adn right give the hegiths right,so plan is to just add them as it will give the diameter,
and we use write ti as node not root as the diameter can be the longest from the node as well