so here in this, you find left sum and right sum and then to add u do maxi=max(maxi,node->val+left+right)
now in the return statement we do return node->val + max(left_sum,right_sum) we do this as, we have to find the max sum, as given in question
in the image, u have 2 paths through 20 and 15, and 20 and 7, you chose the first one only because of the return statement

the return statement, is for the last node, how it should work, assume like that and judge the return stamenet

/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode() : val(0), left(nullptr), right(nullptr) {}
 *     TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
 *     TreeNode(int x, TreeNode *left, TreeNode *right) : val(x), left(left), right(right) {}
 * };
 */

class Solution {
public:
    int maxx = INT_MIN;

    int helper(TreeNode* root) {
        if (root == nullptr) return 0;

        // compute max path sum for left and right subtrees
        int left = max(0, helper(root->left));   // ignore negative paths
        int right = max(0, helper(root->right)); // ignore negative paths

        // update global maximum path sum
        maxx = max(maxx, root->val + left + right);

        // return the max gain from this node to its parent
        return root->val + max(left, right);
    }

    int maxPathSum(TreeNode* root) {
        helper(root);
        return maxx;
    }
};