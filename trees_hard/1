so what we do is we do preorder traversal to check for the nodes,
keep going left and check , or keep going right and chcek, thats all the logic

class Solution {
    public:
    bool getpath(TreeNode* root,vector<int>&arr,int B){
        if(!root) return false;
        arr.push_back(root->val);
        if(root->val==B) return true;
        if(getpath(root->left,arr,B)||(getpath(root->right,arr,B))) return true;
        arr.pop_back(); //pop the leaf node if the ele doesnt exist
        return false; 
    }
    public:
        vector<int> solve(TreeNode*root,int b){
            vector<int>arr;
            if(root=nullptr) return arr;
            getpath(A,arr,b);
            return arr;
        }

}